#### Anton Build Server

##### Todo
* install 
* github trigger push

# install robo
# install phan
# install yarn
# install composer
# everything run in ? (src)
# build trigger github/cronjob/mail ?
# test with php unit